let victoryAmount = rankedCalculator(30, 15)
let heroName = "Frederico"

function rankedCalculator(victory, loss) {
    return victory - loss
}

function rankedLevel(victoryAmount) { 
}

console.log("O herói " + heroName + " tem o saldo de " + victoryAmount + " vitórias")